import { NgxMaskDirective } from './ngx-mask.directive';

describe('NgxMaskDirective', () => {
  it('should create an instance', () => {
    const directive = new NgxMaskDirective();
    expect(directive).toBeTruthy();
  });
});
